n=int(input())
while(n):
	n-=1
	s=input()
	res = ''.join(sorted(s))
	'''c=res[0]
	flag=0
	for i in range(1,len(res)):
		if res[i]==c:
			flag=0
		else:
			flag=1
	if(flag==0):
		print(-1)'''
	if res!=res[::-1]:
		print(res)
	else:
		print(-1)
